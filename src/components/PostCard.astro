---
import type { CollectionEntry } from 'astro:content';
import { getPostUrl } from '@/utils';

interface Props {
  post: CollectionEntry<'posts'>;
  gridCard?: boolean;
}

const { post, gridCard = false } = Astro.props;
---

<li class={gridCard ? 'article grid-item' : 'article'}>
  <article class={`flex-column post-card${gridCard ? ' grid-card' : ' flex-row-lg'}`}>
    <a href={getPostUrl(post.id)} aria-label={post.data.title} class={gridCard ? 'card-img-link' : 'col-item'}>
      <img
        class="post-card-image"
        src={`/assets/img/${post.data.img}`}
        alt=""
      />
    </a>
    <div class={gridCard ? 'post-card-body' : 'col-item post-card-body'}>
      <div>
        <span>
          <i class="far fa-clock"></i>
          <time class="post-date" datetime={post.data.date.toISOString()}>
            {post.data.date.toLocaleDateString('en-GB', {
              day: '2-digit',
              month: 'short',
              year: 'numeric',
            })}
          </time>
        </span>
      </div>
      <h2 style="margin-top: 5px;">
        <a class="post-card-title" href={getPostUrl(post.id)}>
          {post.data.title}
        </a>
      </h2>
      <p>{post.data.shortDescription || post.data.description}</p>
      <ul class="tag-list">
        {(post.data.tags || []).map((tag) => (
          <li class="tag">
            <a href={`/tags/#${tag}`} class="tag-link">#{tag}</a>
          </li>
        ))}
      </ul>
    </div>
  </article>
</li>

<style>
  .flex-column {
    flex-direction: column;
  }

  li.article {
    list-style: none;
    margin-bottom: 3rem;
    transition: all .3s;
  }

  li.article.grid-item {
    margin-bottom: 0;
    display: flex;
    flex-direction: column;
  }

  @media (min-width: 768px) {
    .col-item {
      width: 50%;
      height: 300px;
    }

    .flex-row-lg {
      flex-direction: row;
    }
  }
</style>
